const githubLink = 'https://github.com/albertkawmi/widget-sales-api'

const homeRouteMessage = `Welcome to the Widget Sales API. GET /clients or /sales to see some data. Docs can be found at ${githubLink}`

const dummyError = 'Internal Server Error'

module.exports = {
  githubLink,
  homeRouteMessage,
  dummyError
}
